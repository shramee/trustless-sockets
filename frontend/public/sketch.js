/** Setup */
function setup() {
	createCanvas( 800, 450 );
	frameRate(20);
}

/** Draws frame on canvas. */
function draw() {
	// Sky
	background( 120, 200, 250 );

	// Platform
	rect( 0, 400, 800, 50 )
}